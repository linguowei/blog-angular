import { ArticleStatePipe } from './article-state.pipe';

describe('ArticleStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleStatePipe();
    expect(pipe).toBeTruthy();
  });
});
