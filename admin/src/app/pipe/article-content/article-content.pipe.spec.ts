import { ArticleContentPipe } from './article-content.pipe';

describe('ArticleContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleContentPipe();
    expect(pipe).toBeTruthy();
  });
});
